import { AuthWrapper } from "@/shared/components"
import { LoginFormContainer } from "@/auth/components"

export const Login = () => {
  return (
    <AuthWrapper>
      <LoginFormContainer />
    </AuthWrapper>
  )
}
