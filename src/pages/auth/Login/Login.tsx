import { AuthWrapper } from "@/shared/components"
import { LoginFormContainer } from "./LoginFormContainer"

export const Login = () => {
  return (
    <AuthWrapper>
      <LoginFormContainer />
    </AuthWrapper>
  )
}
