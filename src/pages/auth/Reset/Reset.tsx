import { AuthWrapper } from "@/shared/components"
import { ResetFormContainer } from "@/auth/components"

export const Reset = () => {
  return (
    <AuthWrapper>
      <ResetFormContainer />
    </AuthWrapper>
  )
}
