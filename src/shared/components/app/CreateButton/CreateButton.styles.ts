import { styled } from "@/config"

export const SCreateButton = styled("button", {
  reset: true,
  display: "flex",
  alignItems: "center",
  height: "$40",
  padding: "$0 $24",
  backgroundColor: "$system-black",
  borderRadius: "$8",
  position: "absolute",
  top: "-82px",
  right: "$0",
  cursor: "pointer",
})
