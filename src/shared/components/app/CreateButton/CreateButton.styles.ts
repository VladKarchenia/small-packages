import { styled } from "@/stitches/config"

export const SCreateButton = styled("div", {
  reset: true,
  display: "flex",
  alignItems: "center",
  height: "$40",
  padding: "$0 $24",
  backgroundColor: "$system-black",
  borderRadius: "$8",
  cursor: "pointer",
})
